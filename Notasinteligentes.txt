En este documento tendre en cuenta todas las anotaciones sobre el tema
1. Para unificar un repositorio primero debe existir una carpeta en el pc y un nuevo 
proyecto en github se enlaza con el usuario y contraseña asignados

Para el calculo de numeros multidimensionales se utiliza la libreria numpy
pip install numpy as np

tambien se requiere instalar virutalenv
pip install virtualenv